import 'package:flutter/material.dart';

class VetInfoScreen extends StatelessWidget {
  const VetInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vet & Emergency Info'),
      ),
      body: const Center(
        child: Text('Vet & Emergency Info Screen - Coming Soon!'),
      ),
    );
  }
}