import 'package:flutter/material.dart';

class ManualLogScreen extends StatelessWidget {
  const ManualLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Manual Log Entry'),
      ),
      body: const Center(
        child: Text('Add Manual Log Screen - Coming Soon!'),
      ),
    );
  }
}